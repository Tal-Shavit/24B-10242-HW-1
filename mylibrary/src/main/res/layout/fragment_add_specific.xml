<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragments.AddSpecificFragment">

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="32dp"
        android:textColor="@color/black"
        android:textSize="28sp" />

    <FrameLayout
        android:id="@+id/firstFrame"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/title"
        android:layout_margin="16dp">

        <!-- The border and background of the EditText -->
        <EditText
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@drawable/border_with_label"
            android:clickable="false"
            android:focusable="false" />

        <!-- The TextView acting as the label on the border -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|start"
            android:layout_marginStart="16dp"
            android:layout_marginTop="-10dp"
            android:background="#FFFFFF"
            android:padding="4dp"
            android:text="קטגוריה"
            android:textColor="#000000"
            android:textSize="14sp" />

        <!-- The actual EditText for input -->
        <EditText
            android:id="@+id/editTxtFirstFrame"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginStart="8dp"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="8dp"
            android:background="@android:color/transparent" />

    </FrameLayout>

    <FrameLayout
        android:id="@+id/secFrame"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/firstFrame"
        android:layout_margin="16dp">

        <!-- The border and background of the EditText -->
        <EditText
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@drawable/border_with_label"
            android:clickable="false"
            android:focusable="false" />

        <!-- The TextView acting as the label on the border -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|start"
            android:layout_marginStart="16dp"
            android:layout_marginTop="-10dp"
            android:background="#FFFFFF"
            android:padding="4dp"
            android:text="@string/count"
            android:textColor="#000000"
            android:textSize="14sp" />

        <!-- The actual EditText for input -->
        <EditText
            android:id="@+id/editTxtSecFrame"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginStart="8dp"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="8dp"
            android:background="@android:color/transparent"
            android:inputType="number" />

    </FrameLayout>

    <FrameLayout
        android:id="@+id/thirdFrame"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/secFrame"
        android:layout_margin="16dp">

        <!-- The border and background of the EditText -->
        <EditText
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@drawable/border_with_label"
            android:clickable="false"
            android:focusable="false" />

        <!-- The TextView acting as the label on the border -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|start"
            android:layout_marginStart="16dp"
            android:layout_marginTop="-10dp"
            android:background="#FFFFFF"
            android:padding="4dp"
            android:text="@string/description"
            android:textColor="#000000"
            android:textSize="14sp" />

        <!-- The actual EditText for input -->
        <EditText
            android:id="@+id/editTxtThirdFrame"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginStart="8dp"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="8dp"
            android:background="@android:color/transparent" />

    </FrameLayout>

    <FrameLayout
        android:id="@+id/fourthFrame"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/thirdFrame"
        android:layout_margin="16dp">

        <!-- The border and background of the EditText -->
        <EditText
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@drawable/border_with_label"
            android:clickable="false"
            android:focusable="false" />

        <!-- The TextView acting as the label on the border -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|start"
            android:layout_marginStart="16dp"
            android:layout_marginTop="-10dp"
            android:background="#FFFFFF"
            android:padding="4dp"
            android:text="תאריך הוצאה"
            android:textColor="#000000"
            android:textSize="14sp" />

        <!-- The actual EditText for input -->
        <EditText
            android:id="@+id/editTxtDate"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginStart="8dp"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="8dp"
            android:inputType="date"
            android:background="@android:color/transparent" />

    </FrameLayout>


    <com.google.android.material.button.MaterialButton
        android:id="@+id/calcButton"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/fourthFrame"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:backgroundTint="@color/white"
        android:text="חישוב"
        android:textColor="@color/black"
        app:cornerRadius="8dp"
        app:strokeWidth="1dp" />


</RelativeLayout>